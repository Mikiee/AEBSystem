@using AEBSystem.Core.Models
@{
    ViewBag.Title = "Batch Process";
}
<div class="row wrapper border-bottom white-bg page-heading">
    <h2>Batch Process</h2>
</div>

<div class="wrapper wrapper-content animated fadeInRight">
    <div class="ibox-content">
        <div class="form-group">
            <h3 class="control-label col-md-2">Batch No: </h3>
            <div class="col-lg-4">
                @Html.DropDownList("Status", new SelectList(Enum.GetValues(typeof(BatchProcessDropDown))),
                                  "--Select Status--",
                                  new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group">
           <h3 class="control-label col-md-2">Status: </h3>
            <div class="col-lg-4">
                @Html.DropDownList("Status", new SelectList(Enum.GetValues(typeof(BatchProcessDropDown))),
                                  "--Select Status--",
                                  new { @class = "form-control" })
            </div>
        </div>

    </div>
</div>

